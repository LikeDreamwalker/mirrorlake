{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        // Run the extension in the extension folder, not the root folder
        "--extensionDevelopmentPath=${workspaceFolder}"
        // "--disable-extensions"
      ],
      "outFiles": [
        "${workspaceFolder}/apps/mirrorlake-color-highlighter/dist/**/*.js"
      ]
      // "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
